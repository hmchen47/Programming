#!/usr/bin/python
# _*_ coding = UTF-8 _*_

def lenIter(aStr):
    '''
    aStr: a string
    
    returns: int, the length of aStr
    '''
    total = 0
    for c in aStr:
        total += 1
    return total